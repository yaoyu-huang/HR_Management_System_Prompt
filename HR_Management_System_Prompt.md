---

---

# HR_Management_System_Prompt

## 需求分析

#### **问：一个HR要完成的事情有哪些是可以用python/自动化流消灭的机械重复工作**

答:可以自动化掉的 HR 机械重复工作非常多，核心思路是：规则明确、频率高、出错成本高 的流程最值得先做。

##### 最值得自动化的 HR 工作清单

- 简历收集与初筛
  - 自动从邮箱/招聘平台抓取简历
  - 提取关键信息（学历、年限、技能、城市）
  - 按规则打标签和评分，自动进入人才库
- 面试安排与协调
  - 候选人、面试官、会议室/会议链接自动匹配空闲时间
  - 自动发日历邀请、提醒、改期通知
  - 面试前自动发送资料包（公司介绍、岗位 JD、流程说明）
- 候选人沟通自动化
  - 投递确认、流程进度、结果通知模板化自动发送
  - 统一渠道（邮件/企业微信/钉钉）触发通知
  - 超时未回复自动催办
- Offer 与入职流程
  - Offer 模板自动填充（薪资、岗位、汇报线）
  - 审批流自动触发（部门负责人→HRBP→财务）
  - 入职资料收集清单自动推送与校验缺失项
- 员工主数据维护
  - 新增/转岗/离职信息自动同步到 HRIS、考勤、通讯录、权限系统
  - 字段标准化、去重、异常校验（身份证号格式、银行卡位数等）
- 考勤与假期处理
  - 自动汇总考勤异常（迟到、漏打卡、加班）
  - 假期余额自动计算、冲突规则校验
  - 月度考勤报表自动生成并分发
- 薪酬前置数据准备（非算薪核心）
  - 补贴、绩效、考勤、奖金数据自动拉取与合并
  - 异常值检测（极端加班时长、重复发放）
  - 发薪前核对清单自动生成
- 培训与试用期跟踪
  - 新员工培训计划自动排期和提醒
  - 试用期节点（30/60/90 天）自动触发评估表单
  - 逾期未提交自动升级提醒
- 报表与看板
  - 招聘漏斗、到岗率、离职率、人效等报表定时自动生成
  - 周报/月报自动发给管理层（邮件/IM）
- 合规与文档归档
  - 合同到期预警
  - 必填合规文件缺失提醒
  - 面试记录、审批记录自动归档，便于审计

------

##### 用 Python/自动化流各自适合做什么

- Python 更适合
  - 多数据源清洗、复杂规则匹配、文本解析（简历解析）
  - 自定义报表与异常检测
  - 与 API 深度集成（ATS、HRIS、OA）
- 低代码自动化流（Zapier/Make/Power Automate/飞书多维表自动化）更适合
  - “触发器 + 通知 + 审批”类流程
  - 快速上线、业务同学可维护
  - 跨 SaaS 的轻集成

------

##### 推荐你先做的 3 个“高回报”自动化

1. 面试安排自动化（省协调时间最多）
2. 候选人状态通知自动化（提升候选人体验）
3. 月度 HR 报表自动生成（减少手工汇总出错）

------

如果你愿意，我可以下一步直接给你一个**“HR 自动化落地路线图（2周可上线）”**，按你们现有工具（企业微信/飞书/钉钉/Excel/邮箱）来定制。

------

## 原型系统

#### Prompt1（系统初步功能）

根据你上面提出的这些内容，帮我制作一个HR系统，完成

1.面试安排自动化（省协调时间最多）

2.候选人状态通知自动化（提升候选人体验）

3.月度 HR 报表自动生成（减少手工汇总出错）这三个核心功能，并且该系统可以直接演示使用

------



#### Prompt2（前端美化）

帮我把这个系统做成中文化页面，前端再美化一下，可以加上动态效果或者拼色背景，类似米哈游或者腾讯的网页设计风格

（上述效果不好）

------

\# Visual Style Requirements (Apple Aesthetic)

\1. **Typography & Hierarchy:** 必须追求完美的排版。标题使用非常粗的粗体，副标题和正文使用更细的字重，建立清晰的信息阶梯。默认字体优先选用 San Francisco 或其替代方案（如 Inter）。注意调整行高（leading）和字距（tracking），让文本呼吸。

\2. **Space & Layout:** 拥抱大量的“负空间”（留白）。增加 `padding` 和 `margin`。让组件之间有呼吸感。拒绝拥挤的布局。

\3. **Palette:** 极致清爽。主体使用纯白（#FFFFFF）或接近纯白的底色（如 #F5F5F7），文本使用不同深浅的深灰色（非纯黑，如 #1D1D1F）。关键交互（如按钮）使用一个饱和度适中、干净的蓝色（#0071E3）。

\4. **Motion & Interaction:** 任何交互（hover、加载、状态切换）都必须加上丝滑的 `ease-in-out` 过渡动画（比如 duration 为 200ms 或 300ms）。拒绝生硬的切换。

\5. **Details:** 如果使用阴影，必须是极度微妙、柔和、几乎不可察觉的弥散阴影（Soft, subtle shadow）。如果有卡片，边缘裁切要干净，使用适中的圆角。

\# Stack

使用 React/Vue 3 + Tailwind CSS 构建。请生成干净、语义化且响应式的代码。



------

\# 核心任务

帮我修改当前系统的前端页面。

\# 角色设定

你是一位在 Apple (Cupertino) 工作多年的顶级资深前端架构师与 UI/UX 设计师，深谙 "Human Interface Guidelines" 设计哲学。你极度追求像素级完美（Pixel-perfect）、极简主义、以及令人愉悦的用户体验。你需要使用 React/Vue3 和 Tailwind CSS 来完成开发。

\# Apple 核心设计语言（严格遵守）

\1. **空气感排版 (Airy Typography)**

   \- 字体族：强制首选 `font-sans` 并通过 CSS 变量模拟 "San Francisco" 或 "Inter" 字体。

   \- 标题：使用极具张力的粗体（`font-semibold` 到 `font-bold`），配合紧凑的字间距（`tracking-tight`）。

   \- 正文：使用 `font-normal`，配合宽松的行高（`leading-relaxed`），颜色使用次级灰度（如 `text-gray-500` 或 `text-zinc-500`）以降低视觉噪音。

   \- 层级：通过极端的字号对比（如 `text-5xl` 配合 `text-lg`）建立绝对清晰的视觉焦点。

\2. **极简色彩与材质 (Minimalist Palette & Material)**

   \- 背景色：极其干净的底色。浅色模式使用纯白 `#FFFFFF` 或极浅灰（`bg-zinc-50`）；深色模式使用纯黑 `#000000` 或极深灰（`bg-zinc-900`）。

   \- 强调色：在极简背景下，仅在关键交互点（如主要 Button 或 Link）使用克制的 Apple 蓝（`bg-blue-500`）。

   \- 材质（核心）：大量使用毛玻璃效果（Glassmorphism）。针对悬浮卡片、导航栏等，使用 `bg-white/70` 或 `bg-white/80` 配合 `backdrop-blur-xl` 或 `backdrop-blur-2xl`，以及极细的半透明边框（`border border-white/20`）。

\3. **空间与留白 (Breathing Space)**

   \- 布局：彻底抛弃拥挤感。使用巨大的 Padding 和 Margin（如 `p-8`, `p-12`, `gap-8`）。

   \- 边角：使用圆润、极其平滑的圆角（`rounded-2xl` 或 `rounded-3xl`）。

\4. **克制的光影 (Subtle Elevation)**

   \- 绝不使用深黑色、生硬的阴影。

   \- 卡片悬浮阴影必须是弥散的、极度柔和的（例如使用 Tailwind 的 `shadow-sm` 到 `shadow-lg`，但通过自定义配置将阴影颜色改为极其透明的黑色 `rgba(0,0,0,0.04)`）。

\5. **丝滑微交互 (Fluid Micro-interactions)**

   \- 任何 Hover、Focus 或状态改变，必须带有平滑的过渡动画。

   \- 强制使用 `transition-all duration-500 ease-out` 级别的缓动效果。

   \- 按钮和卡片 Hover 时，可以有极微小的放大（`hover:scale-[1.02]`）和阴影加深效果，但绝不能突兀。

\# 代码交付要求

\- 提供完整、可直接运行的单文件代码（包含必要的样式和结构）。

\- 优先使用 Tailwind CSS 实现所有视觉效果，保持 HTML 结构极度干净、语义化。

\- 组件必须是完全响应式的（Mobile-first），在手机端和宽屏桌面端都有完美的表现。

------



#### Prompt3（增删改功能）

加职位发布

加“记住登录 / 修改密码 / 账号锁定策略”

导入模板下载与导入错误明细报告

统一分页、搜索、筛选、排序（更像正式 SaaS 后台）

将简历筛选部分去除，前端页面加入毛玻璃效果，由你自行设计，要求背景主题颜色为灰白

------

在职位发布该功能下添加选项“发布渠道”，发布渠道举例有“官网”、“BOSS直聘”、“牛客”、”实习僧“；然后给职位列表模块添加职位状态修改功能，用于实时根据hc修改职位招聘状态

## AI建议优化

#### Prompt1（编写HR自动化脚本）

\# 任务：编写一个 Python 面试日程协调脚本 (CLI 自动化工具)

\# 角色

你是一个资深的 HR 自动化效率黑客，擅长消灭繁琐的日程沟通。

\# 核心逻辑

请使用 Python 编写一个可以直接在终端运行的脚本，模拟“无感日程协调”：

\1. **数据模拟**：在代码中伪造一个 JSON 数据，代表面试官下周的空闲时间（比如包含 5 个零散的空闲时段）。

\2. **智能筛选**：编写一个函数，自动从这 5 个时段中，挑选出 3 个最适合面试的时间（比如避开中午休息时间，优先选下午）。

\3. **日程邀请生成**：使用 Python 的 `ics` 库（或其他内置方法），根据选出的最终时间，自动在本地生成一个标准的 `.ics` 日程邀请文件。

\4. **邮件草稿输出**：在终端打印出一封极具专业素养的候选人邀约邮件，邮件中需清晰列出这 3 个时间选项供候选人选择，并提示附件有日程邀请。

\# 约束

代码必须包含清晰的中文注释。包含基础的异常处理逻辑（例如：如果 JSON 里没有空闲时间怎么办）。

------

\# 任务：基于大模型的“千人千面”候选人触达系统

\# 角色

你是懂人性的高级雇主品牌专家兼 Python 极客。

\# 核心逻辑

请编写一个 Python 脚本，将 HR 极其简短、甚至有点直接的面评，转化为一封充满温度和建设性意见的候选人通知信。

\1. **输入模拟**：定义一个字典，包含候选人姓名（张三）、应聘岗位（前端开发）、HR 面评（“技术底子不错，但沟通太闷，且期望薪资超出部门预算”）、结果（淘汰）。

\2. **大模型调用**：使用 `google-generativeai` 库接入 Gemini API。

\3. **System Prompt 设计**：在代码中写一段精彩的 Prompt 指导大模型——要求拒信不能出现“能力不行/沟通差”等冷淡词汇；要把“沟通闷”包装成“您的特质更适合专注深耕的独立技术攻坚岗”，把“薪资超标”包装成“目前公司架构暂时无法提供匹配您资历的发展空间”。

\4. **输出执行**：调用模型，并在终端完整打印出这封高情商的千字邮件。

\# 约束

请在代码顶部留出 `GEMINI_API_KEY = "你的密钥"` 的变量位置。代码要极其健壮，网络请求部分需加入重试或异常捕获机制。

------

\# 任务：HR 数据洞察与高管报告自动生成器

\# 角色

你是精通业务线诉求的 HRBP 兼数据分析工程师。

\# 核心逻辑

写一个完整的 Python 数据处理与分析流：

\1. **造数据**：使用 `pandas` 库，在内存中直接生成一个包含 100 条模拟招聘记录的 DataFrame（需包含字段：候选人ID、岗位来源、初筛耗时、一面结果、最终是否发 Offer）。

\2. **数据聚合**：用 pandas 计算出两个核心指标：“各渠道的最终 Offer 转化率” 以及 “平均招聘周期（天）”。

\3. **洞察生成**：将计算得出的核心数字，拼接成一段文本（例如：“本月内推渠道转化率达 15%，而 Boss 直聘仅为 2%；平均招聘周期为 18天”）。

\4. **AI 报告输出**：再次调用 Gemini API，将拼接好的数据喂给它，要求它以“HR 总监”的口吻，生成一份包含【核心数据表现】、【潜在流程风险（如某环节耗时过长）】和【下月行动建议】的结构化商业报告。

#### Prompt2（根据实际情况修改）

把这三个脚本的功能整合到该系统当中，并且修改为本机用环境变量提供 Key。

gemini的API无法使用，现在改为使用deepseek的API。
## 二次优化

#### Prompt（优化建议）

可以进一步做岗位分类功能，直接做到整个系统的顶部，先选择岗位；

调教ai面评，可以稍微简短精练一点；

在总览看板部分增加岗位状态功能，该岗位候选人目前阶段与该岗位hc。

在候选人管理中添加修改候选人系统职位和应聘说明功能


